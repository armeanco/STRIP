services:
  - type: web
    name: iptv-playlist-scraper
    env: python
    buildCommand: "pip install requests playwright flask && playwright install --with-deps chromium"
    startCommand: "python app.py"
    plan: free
    envVars:
      - key: GITHUB_TOKEN
        sync: false
